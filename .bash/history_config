export HISTSIZE=50000

#ignore duplicates
export HISTCONTROL=ignoreboth

#append to history instead of overwriting
shopt -s histappend

#append across sessions (for screen? we'll see about tmux) 
PROMPT_COMMAND="$PROMPT_COMMAND;history -a; history -n"
